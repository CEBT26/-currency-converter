package com.cebtAlura.currencyConverter;

/*
    Record to take the different currencies
    Argentine pesos (ARS), Brazilian real (BRL),
    and Colombian peso (COP).
 */
public record converter(double ARS, double BRL, double COP) {
}
